# Python 3.11, CUDA 12.x (Colab Tesla T4). Compatible-release pins (~=) lock the
# minor version. For a byte-exact lock, run `pip freeze` in your working
# environment and replace the lines below with == pins.

# deep learning
torch~=2.5.0
torchvision~=0.20.0
timm~=1.0.11

# augmentation — pre-2.0 API (ShiftScaleRotate, CoarseDropout(max_holes=...))
albumentations==1.3.1

# interpretability
grad-cam~=1.5.0

# data / ml
numpy~=1.26.0
pandas~=2.2.0
scikit-learn~=1.5.0
statsmodels~=0.14.0

# visualisation
matplotlib~=3.9.0
seaborn~=0.13.0
Pillow~=10.4.0

# data download & config
kaggle~=1.6.0
PyYAML~=6.0
